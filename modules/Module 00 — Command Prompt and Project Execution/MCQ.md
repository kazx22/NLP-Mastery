# MCQ Exam — Command Prompt Mastery for NLP Projects (Windows CMD)

**Total Questions:** 30

- 10 Beginner
- 10 Intermediate
- 10 Advanced

Choose the best answer for each question.

---

# Beginner MCQs

## 1. What does `dir` do in Windows CMD?

A. Deletes a folder  
B. Shows files and folders in the current location  
C. Runs a Python script  
D. Renames a file

---

## 2. What does `cd` mean?

A. Copy directory  
B. Change directory  
C. Create directory  
D. Check disk

---

## 3. Which command moves one folder up?

A. `cd /`  
B. `cd ..`  
C. `dir ..`  
D. `up`

---

## 4. Which command creates a folder?

A. `mkfolder`  
B. `newdir`  
C. `mkdir`  
D. `create`

---

## 5. Which command deletes a file?

A. `rmdir`  
B. `erasefolder`  
C. `del`  
D. `removefile`

---

## 6. Which command renames a file or folder?

A. `move`  
B. `ren`  
C. `copy`  
D. `dir`

---

## 7. Which path is an absolute path?

A. `Documents`  
B. `data\raw`  
C. `C:\University\Projects`  
D. `..\src`

---

## 8. Which command checks your Python version?

A. `python check`  
B. `python --version`  
C. `pip version`  
D. `version python`

---

## 9. Which command runs a simple Python file?

A. `python -m file.py`  
B. `python run file.py`  
C. `python file.py`  
D. `pip file.py`

---

## 10. In Windows CMD, which slash is normally used in paths?

A. `/`  
B. `\`  
C. `//`  
D. `::`

---

# Intermediate MCQs

## 11. What is the main purpose of a virtual environment?

A. To make your computer faster  
B. To store only notebooks  
C. To isolate project packages and versions  
D. To replace Python

---

## 12. Which command creates a virtual environment named `.venv`?

A. `python create venv .venv`  
B. `python -m venv .venv`  
C. `pip install venv .venv`  
D. `venv .venv`

---

## 13. Which command activates a virtual environment in Windows CMD?

A. `.venv\activate`  
B. `activate .venv`  
C. `.venv\Scripts\activate`  
D. `python .venv\activate`

---

## 14. Which command turns a virtual environment off?

A. `stopvenv`  
B. `deactivate`  
C. `close .venv`  
D. `endenv`

---

## 15. Which command installs pandas safely using the current Python interpreter?

A. `pip pandas`  
B. `install pandas`  
C. `python -m pip install pandas`  
D. `python install pandas`

---

## 16. What does `pip list` do?

A. Lists Python files  
B. Lists installed packages  
C. Lists folders only  
D. Lists Git commits

---

## 17. What does `pip freeze > requirements.txt` do?

A. Freezes the computer  
B. Deletes all packages  
C. Saves installed packages and versions into a file  
D. Activates requirements

---

## 18. Which command installs packages from a requirements file?

A. `pip install requirements.txt`  
B. `pip -r requirements.txt`  
C. `pip install -r requirements.txt`  
D. `python requirements.txt`

---

## 19. What is the difference between `copy` and `move`?

A. No difference  
B. `copy` duplicates, `move` relocates  
C. `copy` deletes, `move` renames  
D. `copy` activates, `move` deactivates

---

## 20. What does `ren` do?

A. Moves a file to another folder  
B. Creates a new file  
C. Changes the name only  
D. Lists file names

---

# Advanced MCQs

## 21. In a project with a `src` folder, which is the preferred command to run `main.py`?

A. `python main.py`  
B. `python src\main.py`  
C. `python -m src.main`  
D. `pip run src.main`

---

## 22. Why is `python -m src.main` often better than `python src\main.py`?

A. It uses less memory  
B. It treats the code as part of the project structure and helps imports work correctly  
C. It deletes errors automatically  
D. It installs missing packages

---

## 23. In `python -m src.benchmark --model biobert`, what is `--model`?

A. A Python keyword  
B. A folder name  
C. An argument name  
D. A package manager command

---

## 24. In `python -m src.benchmark --model biobert`, what is `biobert`?

A. The argument value  
B. The Python interpreter  
C. The folder path  
D. The virtual environment

---

## 25. What does `'python' is not recognized` usually mean?

A. Python code has a syntax error  
B. CMD cannot find the Python executable  
C. Your file name is wrong  
D. Git is not installed

---

## 26. Which command helps check which Python CMD is using?

A. `show python`  
B. `where python`  
C. `find python`  
D. `python --where`

---

## 27. If you installed a package but Python still says `ModuleNotFoundError`, a common reason is:

A. Your monitor is not connected  
B. `pip` and `python` are pointing to different environments  
C. The folder name is too short  
D. You used `dir` before `cd`

---

## 28. What does `rmdir /s folder_name` do?

A. Renames the folder  
B. Deletes the folder only if empty  
C. Deletes the folder and everything inside it  
D. Copies the folder structure

---

## 29. If a folder name contains spaces, what is the safest way to change into it?

A. `cd My Folder`  
B. `cd <My Folder>`  
C. `cd "My Folder"`  
D. `cd [My Folder]`

---

## 30. Which workflow is best for running an NLP project?

A. Open any folder and run random commands  
B. `cd` to project root → activate `.venv` → run `python -m src.file`  
C. Install everything globally and skip virtual environments  
D. Run scripts from inside `.venv\Scripts` directly

---

# Answers

## Beginner

1. **B**
2. **B**
3. **B**
4. **C**
5. **C**
6. **B**
7. **C**
8. **B**
9. **C**
10. **B**

## Intermediate

11. **C**
12. **B**
13. **C**
14. **B**
15. **C**
16. **B**
17. **C**
18. **C**
19. **B**
20. **C**

## Advanced

21. **C**
22. **B**
23. **C**
24. **A**
25. **B**
26. **B**
27. **B**
28. **C**
29. **C**
30. **B**

---

# End
